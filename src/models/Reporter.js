/**
 * Identidad de sesión (no es colección permanente).
 * Se documenta aquí para claridad del dominio.
 */
module.exports = {
  roles: Object.freeze({
    REPORTER: 'reporter',
    ADMIN: 'admin',
  }),
};
